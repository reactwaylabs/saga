{
  "name": "simplr-flux",
  "main": "dist/simplr-flux.js",
  "version": "2.0.0-alfa",
  "scripts": {
    "build": "webpack",
    "tslint": "tslint --project . --config ./tslint.json",
    "watch": "webpack --watch",
    "test": "tsc -p . --noEmit && npm run tslint",
    "uglifyjs": "uglifyjs ./dist/action-emitter.js -o ./dist/action-emitter.min.js --compress --mangle",
    "release": "npm run build && npm run uglifyjs"
  },
  "devDependencies": {
    "@types/flux": "^3.0.1",
    "ts-loader": "^2.0.3",
    "tslint": "^5.0.0",
    "typescript": "^2.2.2",
    "uglifyjs": "^2.4.10",
    "webpack": "^2.3.3"
  },
  "dependencies": {
    "flux": "^3.1.2",
    "immutable": "^3.8.1"
  }
}
